<form method="post" action="{{ path('app_origin_country_delete', {'id': origin_country.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ origin_country.id) }}">
    <button class="btn">Delete</button>
</form>
